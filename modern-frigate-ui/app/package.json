{
  "name": "modern-frigate-ui-app",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npm --prefix frontend run build && npm --prefix backend run build",
    "dev": "npm --prefix backend run dev"
  }
}
